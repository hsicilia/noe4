/**
 * pitmar.js
 *
 * Declaraciones generales utilizadas en la página
 */


$(document).ready(function() {

	// Menú Superfish
	var menu = $('#menu').superfish({
		//add options here if required
	});
});